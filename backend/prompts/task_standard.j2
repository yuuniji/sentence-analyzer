【待解析句子】
{{ sentence }}

【<details> 标题文本】（直接使用，不得修改）
{{ trunk_text }}

【解析指令】
请对上述英语句子进行完整的五模块深度解析。

重要提醒：
1. <details><summary> 中必须使用上方提供的"标题文本"，不得自行修改
2. 意群拆分的 / 分隔符前后各留一个半角空格
3. 语法枝叶分析的编号使用带圈数字（①②③④⑤）
4. 所有 Markdown 表格必须包含表头分隔行（|---|---|）
5. 输出必须以 </details> 结束，之后不得有任何内容

{% if context_sentences %}
【上下文参考】
本句出自以下原著语境，仅供理解代词指代和语意逻辑：
{{ context_sentences }}

⚠️【核心警告】
你**必须且只能**对上方的【待解析句子】进行拆解和翻译！
**绝对不要**去解析【上下文参考】中的其他句子，上下文仅作辅助理解用途！

【书籍排版精读指令】
如果【待解析句子】或【上下文参考】中存在带有 Markdown 标记的格式（例如 *斜体* 或 **加粗**），这代表了原著中的特殊排版格式。
请在后续的「重难点与语法分析」或「词汇」板块中单独指出，并深度剖析原著作者使用此格式是为了强调特定概念、体现情感变化还是做语义对比。
（重要提醒：你可以使用正常的 Markdown 语法，但绝对不要在代码块（如 ` ` 或 ```）内部混合使用 *斜体* 或 **加粗**，因为代码块会让这些排版失效！如果要展示斜体，请直接输出 *斜体词汇*。）
{% endif %}

{% if custom_terms %}
【自定义术语表】（以下术语请使用指定译法）
{% for term, translation in custom_terms.items() %}
- {{ term }} = {{ translation }}
{% endfor %}
{% endif %}

请开始解析。
注意：在输出正式解析结果（<details> 标签）之前，请先用 HTML 注释 `<!-- 思维推导：... -->` 包裹你的推导过程。
在推导过程中，请按以下步骤思考：
1. 找出全句的核心主谓宾（或主系表）
2. 识别所有从句、非谓语动词和介词短语等修饰成分，并明确其修饰关系
3. 结合上下文（若有）推断代词指代和特殊排版意图
4. 思考如何将复杂的英文结构转化为流畅的中文

推导完成后，再换行输出完整的 `<details>` 格式解析：
